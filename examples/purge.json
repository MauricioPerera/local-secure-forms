{
  "operation": "purge",
  "purpose": "Eliminar permanentemente un recurso sin posibilidad de recuperación",
  "risk": "irreversible",
  "initiator": "agent",
  "fields": [
    {"name": "resource_ref", "type": "opaque_reference", "sensitivity": "private", "required": true}
  ],
  "validation": {"preflight": "resource_is_in_trash"},
  "confirmation": {"method": "pin_and_totp", "required": true, "single_use": true, "summary": ["resource_ref"]},
  "expires_in_seconds": 120
}
